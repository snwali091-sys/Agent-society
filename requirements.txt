# Core LLM / API
openai>=1.30.0          # OpenAI-compatible SDK (works with Qwen Cloud)

# Web API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Utilities
python-dotenv>=1.0.0   # Load API keys from .env file
httpx>=0.27.0          # Async HTTP for agent tool calls

# Optional: persistence layer (swap SharedMemory for Redis in production)
# redis>=5.0.0
